// Name: Regie Daquioag
// Date: October 8, 2015
// Project: Team string Assignment

#include <iostream>
#include <cassert>
using namespace std;

bool isPalindrome(string strA); //A

int main()
{
    assert(isPalindrome("hi") == false);
    assert(isPalindrome("x") == true);
    
    return 0;
}

//A
bool isPalindrome(string strA)
{
    int length = strA.length();
    
        if (length == 1)
        {
            return true;
        }
        else 
        {
            return false;
        }   
}

